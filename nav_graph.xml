<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/misCitasFragment">

    <!-- Fragmento de MisCitasFragment -->
    <fragment
        android:id="@+id/misCitasFragment"
        android:name="com.example.vetcare.fragmentos.MisCitasFragment"
        android:label="Mis Citas"
        tools:layout="@layout/fragment_mis_citas">

        <!-- Acción para navegar desde MisCitasFragment a VerMapaFragment -->
        <action
            android:id="@+id/action_MisCitasFragment_to_verMapaFragment"
            app:destination="@id/verMapaFragment" />
    </fragment>

    <!-- Fragmento de VerMapaFragment -->
    <fragment
        android:id="@+id/verMapaFragment"
        android:name="com.example.vetcare.fragmentos.VerMapaFragment"
        android:label="Ver Mapa"
        tools:layout="@layout/fragment_ver_mapa" />
</navigation>